
# Version of the configuration file
version: 1.1

# Name of the pipeline
extra_pip_packages: stpt-pipeline
name: stpt

# I/O
root_dir: /data/meds1_a/imaxtapp/incoming/MA1-0001/STPT/MA1-190726-1558
output_dir: /data/meds1_b/processed/debug

# Recipes to run
recipes: ["distortion", "mosaic", "downsample", "tiff"]

# Resources requested
resources:
  threads: 1
  workers: 20
  memory: 24

